# Extra dependencies for the legacy Streamlit demo (app.py) only.
# Not needed by the FastAPI backend (api/) — install on top of
# requirements.txt: pip install -r requirements.txt -r requirements-streamlit.txt
streamlit==1.32.2
streamlit-lightweight-charts==0.7.20
matplotlib==3.8.2
